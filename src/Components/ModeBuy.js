import React, {useEffect} from 'react'

const ModeBuy = () => {

    useEffect(() => {
        // eslint-disable-next-line
    }, [])

    return (
        <main>
            <div style={{flex: 1}}>

                <h3 className="title-block row-group">Купить у бота</h3>

            </div>
        </main>
    )
}

export default ModeBuy